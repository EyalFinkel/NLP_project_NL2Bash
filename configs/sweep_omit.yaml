command:
- ${env}
- ${interpreter}
- ${program}
- "--config"
- "config.yaml"
- ${args}

program: main.py
entity: yuval_eyal
project: nl2bash

method: grid
parameters:
  omit_rate:
    values: [0, 0.05, 0.1, 0.15, 0.2, 0.25, 0.3, 0.35, 0.4, 0.45, 0.5]